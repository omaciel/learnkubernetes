apiVersion: v1
kind: Service
metadata:
  name: mariadb-service
spec:
  ports:
  - nodePort: 30963
    port: 3306
    protocol: TCP
    targetPort: 3306
  selector:
    app: mariadb
  sessionAffinity: None
  type: NodePort
